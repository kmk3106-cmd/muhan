# 밸류 리밸런싱 (VR 5.0) — 구현 사양서 (**운영중**)

> 상태: **구현·배포 완료, 실계좌 운용중.** NH PLUG 예약주문으로 2주 사다리 일괄 제출.
> 첫 실전: 2026-08-22 VR 5기 49주차(8/24~9/4) 22건 접수 — 라오어 공개표와 **전 항목 센트 일치**.
> 출처: 사용자 제공 — 라오어 네이버카페 VR 5.0 (V1~V4 폐기, 5.0 단일 버전, 운용유형만 분기).
> ⚠️ VR은 무한매수법/떨사오팔/종사종팔(트렌치형)과 **완전히 다른 알고리즘** — ddsop 복제 불가, 신규 구현.
>
> **산출식 전문은 §6.9. 실전 검증 이력·함정은 §6.95.**

---

## 1. 개요

밸류 리밸런싱(Value Rebalancing) = 우상향하는 **목표 밸류선 V** 에 주식 평가금을 주기적으로 맞추는 방법.
평가금이 V 위로 일정밴드 벗어나면 **매도**(현금 Pool 확보), 아래로 벗어나면 **매수**(Pool 사용). 밴드 안이면 거래 없음.
2주에 1회 판단. 종목은 TQQQ 등 레버리지 ETF 중심. **핵심은 매도** — 상단 돌파 시 주저 없이 매도해 Pool 확보.

운용 유형 3종(파라미터만 다름): **적립식 / 거치식 / 인출식**.

---

## 2. 핵심 용어 & 공식 (사용자 제공 — 확정)

출처: posts/009 (2022-04-15). 핵심: TQQQ 평가금을 가상 V 곡선에 맞춰 흐르게 하고 벗어나면 리밸런싱.
올인과 달리 평균 현금 13~32% 보유하면서 레버리지 수익 확보.

| 기호 | 의미 (확정) |
|---|---|
| **V** | Value. TQQQ 평가금의 기준선. 상승률 공식으로 2주마다 갱신 |
| **P (= Pool)** | **보유 현금** (P와 Pool은 동일 — 매수 재원, 매도 시 적립) |
| **밴드** | V × 0.85 ~ V × 1.15 (±15%). 벗어나면 매수/매도로 밴드 내 복귀 |
| **G (Gradient)** | V 기울기 분모. 분모 클수록 V 성장↓ → 안정적 |
| **상승률** | 다음 V에 더해지는 값 = **P/G** |
| **사이클** | 2주 1회. **V·매수표·매도표 모두 갱신** |

**핵심 공식 (확정):**
```
다음 V = 현재 V + P/G + 적립금(적립식) / − 인출금(인출식)
밴드 상단 = V × 1.15   ·   밴드 하단 = V × 0.85

평가금 > 상단  → 매도
평가금 < 하단  → 매수 (Pool 사용 한도 내)
밴드 안        → 거래 없음
```
**예시:** 매 사이클 $250 적립, 현재 V=$9,000, P(Pool)=$1,000, G=10
→ 다음 V = 9,000 + 1,000/10 + 250 = **$9,350**

### G 값 가이드 (TQQQ 3배 기준)
| G | 수익 레버리지 | 위험 레버리지 | 평균 현금보유 |
|---|---|---|---|
| /10 | 2.98배 | 2.37배 | 13.2% |
| /20 | 2.74배 | 2.18배 | ~21% |
| /30 | 2.53배 | 2.03배 | ~27% |
| /40 | 2.38배 | 1.91배 | 31.84% |

- 권장 시작: **/10**(적립식·거치식) / **/20**(인출식). 1년 단위로 /11→/12→… 점진 안정화. 금액 커지면 /30+ 권장.
- 밴드폭: **±15% 오피셜**, ±10%/±5%/±20% 커스텀 허용. **밴드폭 영향은 매우 미세 — 핵심은 G값.**

---

## 3. 운용 절차 (2주 사이클) — 전 유형 동일

### (1) 사이클 시작 — 새 V 계산 + 밴드 재설정
```
다음 V = 현재 V + P/G + 적립금(적립식) / − 인출금(인출식) / +0(거치식)
상단 = V × 1.15   ·   하단 = V × 0.85   (±15% 오피셜)
```

### (2) 매수/매도 판단 (평가금 = TQQQ 보유수 × 현재가)
- **평가금 > 상단(1.15V)** → **매도: 평가금이 V로 돌아올 만큼** 매도 → 받은 현금 **Pool 편입**
- **평가금 < 하단(0.85V)** → **매수: 평가금이 V에 도달할 만큼** 매수 (단 **Pool 사용 한도** 내: 적립식 75% / 거치식 50% / 인출식 25%)
- **밴드 안(0.85V~1.15V)** → 매수·매도 **없음** (거래 없는 사이클)

→ **리밸런싱 목표 = V(중심선)**. 밴드는 거래 발동 트리거일 뿐, 되돌리는 목표는 항상 V.

### (3) 예약주문 (매수표/매도표)
- 사이클 첫날 **매수점·매도점을 지정가 또는 LOC로 2주치 예약** → 사이클 중 가격 도달 시 자동 체결
- 라오어는 사이클 첫날 매수표/매도표(모델 포트폴리오 "홍길동씨")를 공유. 예약가보다 유리하게 체결돼도 사용 OK(posts/031)
- ※ **시스템 구현**: 미국 예약주문은 당일유효 → 워커가 매 거래일 매수점/매도점 지정가를 **자동 재제출**(2주 유지 효과). §6 참조.

### (4) Pool 소진 대처
- Pool 소진 시 하락장에도 매수 불가 → **대기**. 적립식은 다음 적립금으로 복구, 거치·인출식은 상승→매도로 Pool 복구될 때까지 홀드.

### (5) 매도가 핵심 (posts/021, 014)
- 밴드 상단 돌파 시 **주저 없이 매도해 Pool 확보**. **평단보다 낮아도 V 공식상 매도 신호면 매도.**

### (6) 사후 관리
- 매도가 덜 걸리거나 과매수된 경우 다음 사이클 V 갱신에서 자연 조정. 유형 전환은 수개월 단위·사전계획만(2주 단위 금지).

### 구현 관점 매수점/매도점·수량 산출식 (보유수 S, 현재가 무관·트리거가 기준)
> 평가금을 V로 되돌리는 단일 트리거 1쌍(사다리 아님 — 확인 §7-3):
- **매도점가** = 1.15V / S → 그 가격에서 평가금=1.15V. **매도수량** = S − V/(1.15V/S) = S × (1 − 1/1.15) ≈ **S × 0.1304** (평가금 V로 복귀)
- **매수점가** = 0.85V / S → **매수수량** = V/(0.85V/S) − S = S × (1/0.85 − 1) ≈ **S × 0.1765**, 단 매수금액 = 매수수량 × 매수점가 ≤ **Pool × 한도%**(초과 시 한도까지만)

---

## 4. 유형별 파라미터

| 항목 | 적립식 | 거치식 | 인출식 |
|---|---|---|---|
| 자금 흐름 | 매 사이클 **+적립**(월급 연동) | 없음(월급 영향 X) | 매 사이클 **−인출**(최종 목표 단계) |
| Pool 사용 한도 | 적립 후 Pool의 **75%** | Pool의 **50%** | 인출 후 Pool의 **25%** |
| 권장 G | /10 시작 | /10 시작 | /20 시작 |
| 난이도 | 쉬움 | 보통 | 어려움 |
| 라오어 중계 | VR 2~4기 | VR 0~1기 | VR 5기(2025-07~) |

- **Pool 사용 한도 = 한 사이클 매수 시 사용 가능한 현재 Pool의 비율 상한** (적립식은 적립 후·인출식은 인출 후 잔액 기준). 운용하며 점차 낮추는 방향 권장(공격/방어 선택 가능).
- 시스템·공식·매수매도는 **세 유형 완전 동일**, 파라미터(자금흐름·Pool한도·G)만 다름. 유형 전환 자유(단 수개월 단위, 2주 단위 금지).

### 유형별 상세
- **적립식**: 다음 V = V + P/G **+ 적립금**. Pool 75%. 권장 G /10(1년차 /10→/11→/12…). 월급 기반 Pool 빠른 충전, 하락장 적립금 복구. (posts/009·019·020)
- **거치식**: 다음 V = V + P/G (적립·인출 0). Pool 50%(적립식보다 보수적). 월급 무관·외부충격 강함. (posts/009·020)
- **인출식**(최종 목표): 다음 V = V + P/G **− 인출금**. Pool 25%(가장 보수적). 권장 G **/20 시작 → /30**. 원금 점차 인출, 수익분만 잔존. VR 5기(2025-07~, 원금 $2만, 팬딩 중계). (posts/009·028)

### 유형 전환 (posts/020 — 시스템 동일, 자금흐름만 차이라 전환 수월)
- **적립식→거치식**(적립 부담 시): 기존 TQQQ 개수를 거치식 모델 포트폴리오 개수에 맞춤. 남는 TQQQ 자유 처리(홀드/매도).
- **거치식→적립식**(여력 생김): 적립식 목표 N배수로 TQQQ 개수 조정(모자라면 추가 매수).
- **거치식↔인출식**(하락장 일시): 인출 부담 시 잠시 거치식처럼 운영, Pool 회복되면 인출식 복귀.
- 원칙: **수개월 단위만, 2주 단위 금지, 사전 계획 필수.** 적립식+거치식 병행이 비중 조절에 가장 유리.

---

## 5. 공통 원칙

- 사이클: **2주 1회**
- 종목: **TQQQ**(레버리지 ETF) 중심 ⚠️ *현재 TQQQ는 무한매수법 보유 — 티커 무중복 충돌, 실거래 등록 시 결정(사용자: "일단 VR 개발하고 결정")*
- 밴드폭: ±15% 오피셜 (±10% 등 커스텀 허용, 영향 미세)
- 유형 전환: 수개월 단위 OK, **2주 단위 금지**
- **핵심은 매도** — 상단 돌파 시 주저 없이 매도해 Pool 확보
- 길게 가는 방법론 — 무리 없는 페이스가 가장 중요

---

## 6. 시스템 구현 관점 (trading_suite 적용)

- **미국주식 예약주문은 "당일 유효"** (미체결 장마감 후 자동취소, 휴장일만 익일 이월 — KIS 확인 완료). 따라서 "2주 한 번 걸고 방치"를 KIS 네이티브 기간예약으로는 못 함.
- → **워커가 매 거래일 매도점/매수점 지정가를 자동 재제출**해서 "2주 예약 방치" 효과를 동일하게 구현 (기존 전략의 일일 재제출 패턴 재사용).
- **V·Pool·사이클 상태**는 신규 데이터모델 필요 (트렌치 아님). 사이클 경계(2주)에 V 갱신 + 매도점/매수점 재계산.
- kis_client는 검증본 **바이트동일 복제**, 주문은 `order()` 지정가(LOC 또는 장중 §7-5). 매도 MOC는 손절 아닌 리밸런싱이라 해당 없음.

---

## 6.5 증권사 API 선택 — **KIS 전용** (NH/나무 부적합 확인, 2026-06)

VR도 기존 전략과 **동일하게 한국투자증권(KIS) REST API, 단일 계좌 69567573** 으로 구현한다. 다른 증권사 검토 결과:

**나무(NH투자증권) QV Open API — 미국 자동매매 부적합 (사용자 제공 SDK `Downloads/openapi.nm` 실측)**
1. **방식**: WMCA **윈도우 32비트 DLL(C/C++)** — `wmcaQuery()` 호출, 서버 `wmca.nhsec.com:8200`, bin에 `wmca.dll`·`SKComm*.dll`·`CloudNPKI.dll`(공인인증), 샘플은 VC++ 전용. **REST/Python/Linux 아님** → 우리 리눅스 NCP 서버에서 직접 불가(윈도우 PC 상시가동 + DLL 래퍼 브리지 필요).
2. **해외주식 주문 미지원**: 주문 샘플 헤더 `trio_ord.h` 해외 키워드 0건, `주문_SPEC.doc`(UTF-16 스캔) 해외·미국·나스닥·뉴욕·외화·USD **전부 0건** → 이 Open API는 **국내주식 주문 전용**. (나무 *앱*은 미국 주문 되지만 *API*는 안 됨. 시세 쪽만 '해외' 4건=해외지수류)
3. **TR 단종 예정**: KRX 대체거래소(SOR/통합시세) 개편으로 기존 TR·실시간 전문 **2026년 순차 종료** 예정 → 유지보수 리스크.

**기타 증권사**: 키움(구 OpenAPI+ 윈도우 COM, 해외 후발)·LS XingAPI·대신 CYBOS 등도 윈도우 COM/국내 중심. **미국 ETF 자동매매 REST로는 KIS가 사실상 표준**(라오어 생태계·우리 시스템 모두 KIS).

→ ~~**결론: VR은 KIS REST로만 구현. NH/나무·타사 전환은 비권장**~~
> ⚠️ **이 절은 2026-08 폐기됨.** 마지막 문장의 단서("NH가 신형 REST API를 내놓으면 재검토")가
> 실제로 실현됐다. **NH PLUG OpenAPI**(REST, `api.nhplug.com:8443`)가 미국주식 주문을 지원하고,
> 결정적으로 **기간잔량 예약주문**(`bkg_orr_sta_dt~end_dt` + `bkg_orr_tp_cd=2`)이 가능하다.
> KIS는 미국 예약주문이 **당일유효**뿐이라 "2주 걸고 방치"라는 VR의 핵심 운용을 재현할 수 없다.
> → **VR은 NH PLUG 전용으로 구현·운영중.** KIS는 트렌치형 3전략 전용으로 남는다.

---

## 6.9 🔓 실공식 (라오어 표 역산 → 실전 전수 검증 완료)

사용자 제공 팬딩 캡쳐(VR 0기 256주차 · VR 5기 47주차)로 산출식을 역산하고,
2026-08-22 공개된 **VR 5기 49주차 표와 22행 전량을 센트 단위로 재현**해 확정했다.
(§2의 개요 공식 `V+P/G±적립`은 요약본. 실공식은 모멘텀 항이 붙은 아래 형태)

### 입력 5개
| 기호 | 뜻 | 출처 |
|---|---|---|
| `V₁` | 직전 주기 목표 밸류선 | 직전 주기 산출값 |
| `E` | 직전 주기 **마감 평가금** | 모델 잔여 × 주기 마지막날 **종가** |
| `Pool₁` | 직전 주기 마지막 현금 Pool | 주기중 체결 반영된 잔액 |
| `C` | 적립(+)/인출(−) 금액 | 기수 설정값 (5기 −$75/주기) |
| `G` | Gradient (기수 고유) | 0기 16, 5기 41 · 26주마다 +1 |

### 산출 순서 (①→⑥)

```
① 다음 V    = V₁ + Pool₁/G + (E − V₁)/(2√G) + C
              └상승분┘   └── 모멘텀 보정 ──┘  └적립·인출┘

② Pool 이월 = Pool₁ + C

③ 밴드      = 하단 V×0.85 / 상단 V×1.15        (밴드폭 ±15%)

④ 매수점 k  = 하단 ÷ (잔여 + 단위×(k−1))        k = 1,2,3…
   체결 시   Pool −= 단위 × 매수점 ,  잔여 += 단위

⑤ 매도점 k  = 상단 ÷ (잔여 − 단위×(k−1))
   체결 시   Pool += 단위 × 매도점 ,  잔여 −= 단위

⑥ 단수 정지
   매수 : 누적 매수금 ≤ Pool 이월 × 매수한도%   (0기 25% / 5기 10%)
   매도 : 기수별 설정 단수                       (0기 11 / 5기 14)
```

**④⑤의 직관** — 매수점 k는 "지금 사는 중인 그 시점의 잔여수량"으로 하단을 나눈 값이다.
즉 `k=1`은 아직 아무것도 안 산 상태(잔여 216)의 가격이고, 표의 `잔여개수` 칸은
그 단이 **체결된 뒤** 값(218)이라 한 칸 밀려 보인다. 매도도 같은 구조로 반대 방향.

> 결국 `가격 × 잔여수량 = 밴드 경계`가 항상 성립한다. 값이 떨어져 평가금이 하단에
> 닿으면 매수해서 다시 밴드 안으로, 올라서 상단에 닿으면 매도해서 밴드 안으로 되돌린다.
> **가격을 예측하는 게 아니라, 어느 가격이 오든 평가금을 V 근처로 되돌리는 격자**다.

### 배수(mult) 처리
가격 산출은 **전부 모델 수치**(모델 잔여·모델 Pool)로 한다. 배수는 **주문 수량에만** 곱한다.
→ 5기 ×6배수: 모델 2개씩 → 실주문 12주. 배수를 바꿔도 가격표는 그대로.

### 검증 결과
| 기수/주차 | V | 밴드 | 매수 | 매도 | 결과 |
|---|---|---|---|---|---|
| 0기 256주차 | 30,905.34 | ✅ | 8단 | 11단 | 전 항목 일치 |
| 5기 47주차 | 15,015.07 | ✅ | 8단 | 15단 | 매수점2 **1센트** 차 |
| **5기 49주차** | **15,212.59** | ✅ | **8단** | **14단** | **22행 전량 센트 일치** |

- 47주차의 1센트 차(raw 58.54495 → 표 58.55)는 엑셀 반올림 누적. 대조 시 ±$0.01 허용.
- 49주차는 오차 0. 종가 파싱 버그(§6.95) 수정 후 완전 일치.
- **매도 단수**는 표시 단수가 아니라 실제 제출 단수다. 5기는 15 → **14로 정정**(49주차 공개표 기준).
  49주차 기준 `잔여 216 ÷ 1.15 = 187.8`, 14단 후 잔여 188에서 정지 → 이 규칙일 가능성이 높으나
  0기(11단)에는 맞지 않는다. **기수별 설정값으로 두고 공개표에 맞춰 조정** 중.

---

## 6.95 실전 검증 이력 & NH API 함정 (2026-08-22 첫 제출)

### 첫 실전 결과
VR 5기 49주차(8/24~9/4) 22건 — 매수 8건 $56.22~59.86 / 매도 14건 $80.99~92.08, 전건 12주.
NH 접수번호 890~912, 전건 **지정가 · 잔량기준기간예약주문 · 상태 접수**. DB↔NH 대조 불일치 0건.

### 잡은 버그 3개 (전부 이날 실제 데이터로 발견)

**1. NH 일봉에서 시가를 종가로 파싱** (`nh_client.daily_closes`) — 치명
```
{"trade_date":"20260821","open_prc":71.7,"high":71.83,"low":69.91,"close_prc":71.17}
                          ↑ 'prc' 부분일치로 여기 먼저 걸림      ↑ 정답
```
E가 틀리면 V·밴드·22개 가격이 전부 어긋난다. 실제로 E 15,487.20(오) vs 15,372.72(정),
V 15,221.53(오) vs 15,212.59(정)로 벌어졌다.
→ **종가/날짜 필드는 우선순위 지정 목록으로만 정확 매칭**하고 open/high/low 계열은 배제.

**2. rsp_cd `00218`을 실패로 오판** — 예약조회 전체 불가
`00218 = "계속 조회시 다음(연속조회) 버튼을 누르시기 바랍니다"`는 **데이터 정상 + 뒷장 있음**
안내지 오류가 아니다. SDK 기본 성공코드 목록에 없어 조회가 통째로 예외 처리됐다.
→ `NHPLUG_SUCCESS_CODES`(SDK 확장점)로 00218 추가. 벤더링 SDK 원본은 미수정.

**3. 예약조회가 15건에서 잘림**
NH 응답 봉투에 연속조회 커서 필드가 없다(`Output_0`/`rsp_cd`/`rsp_msg` 뿐).
→ 문서화된 `sby_dit_cd`로 **매도(1)·매수(2) 분리조회 후 병합**. 접수번호로 중복 제거.
VR 사다리는 한쪽이 15건을 넘지 않아 안전. 넘으면 경고 로그를 남긴다.

### 운용 교훈
- **종가는 매 주기 산출의 단일 진입점**이다. 여기가 틀리면 나머지가 전부 조용히 틀린다.
  → 미리보기 화면에 `(자동: MM/DD 종가 $X × N주)` 근거를 항상 노출한다.
- NH는 **HTTP 200 ≠ 업무 성공**이고, 반대로 **업무 성공인데 오류처럼 보이는 코드**도 있다.
  성공코드 allowlist는 실거래로만 채워진다.
- 계좌 실보유가 모델 잔여와 어긋날 수 있다(수동 매매 흔적). 가격 산출은 모델 기준이라
  영향 없고, 매도 가능 수량만 확보되면 정상 동작한다.

### 운용 모델 (확정)
- NH-A(…635) = **VR 5기 인출식 ×6배수** (모델 216주×6≈실보유 1,308) — 매수한도 10%, 2개씩×6=12주 단위, 인출 $75/주기(현재)
- NH-B(…977) = **VR 0기 거치식 ×1배수** (모델 430주≈실보유 422) — 매수한도 25%, 4개씩 단위
- 두 기수는 2주 주기가 **한 주씩 엇갈림** → 매주 토요일 그 주 시작 기수 1개 갱신
- 주문 집행: NH PLUG **예약주문**(reservedSubmit, `bkg_orr_sta_dt~end_dt` 기간 + `bkg_orr_tp_cd=2 잔량주문`)으로
  2주치 사다리 일괄 예약 → 2주 방치 (KIS 당일유효 제약 없음)
- 가격은 **모델 수치**(모델 잔여·모델 Pool)로 산출(배수 무관), 주문 수량만 ×배수
- 1단계 운용: 자동 산출 + 팬딩 표 자동 대조(±1센트) 검증 모드 → 수 주기 일치 확인 후 완전 자동

## 7. ✅ 미확정 항목 — 전부 해소 (2026-08)

당초 5개 미확정은 실계좌 등록·라오어 표 역산·첫 실전으로 모두 확정됐다.

| # | 당시 미확정 | 확정 결과 |
|---|---|---|
| 1 | V 초기값·초기 주식수·Pool 시작액 | **신규 산정 불필요.** 진행중 기수를 이어받음 — 직전 주차 V·Pool·모델 잔여를 그대로 등록 (5기 47주차 / 0기 256주차부터 인수) |
| 2 | 사이클 경계 | **격주 월요일 시작 ~ 2주 뒤 금요일 종료.** 평가금·표 갱신은 **직전 금요일 종가** 기준, 토요일에 산출·제출 |
| 3 | 매수/매도표 단수 | **다단계 사다리 확정.** 단일 1쌍 아님 — 매수는 한도%까지, 매도는 기수 설정 단수 (§6.9 ⑥) |
| 4 | 주문 타입 | **지정가(`nmn_pr_tp_cd=00`) + 기간잔량(`bkg_orr_tp_cd=2`)**. LOC 아님 — 2주 기간 내내 상시 유효해야 함 |
| 5 | 시작 파라미터 | 0기 거치식 G=16·한도25%·4개단위·×1배수 / 5기 인출식 G=41·한도10%·2개단위·×6배수·인출 $75 |

> 남은 관찰 항목: **매도 단수 규칙**(기수 설정값으로 운용중, 공개표 대조로 확정 진행 — §6.9)

---

## 8. 구현 현황 (완료 — 운영중)

| 단계 | 내용 | 상태 |
|---|---|---|
| P0 | §7 미확정 사양 확정 | ✅ 전항 해소 |
| P1 | `strategies/vr` 패키지 + NH PLUG SDK 벤더링 + 데이터모델(gisu·weekly_history·reserved_orders·fills·acct_snapshot) | ✅ |
| P2 | 코어 로직 `vr_logic.py` — V 갱신·밴드·매수/매도 사다리·한도 적용 | ✅ 라오어 표 대조 검증 |
| P3 | 워커 — 기수 동기화·스냅샷·자동제출·주기 롤오버 | ✅ |
| P4 | 부모 통합 — mount(`/vr`)·suite_metrics VR 브랜치·killswitch·deploy 보존 | ✅ |
| P4.5 | 프론트 — VR 전용 메뉴(카드·설정·미리보기·제출·원장), 대시보드 계좌필터 [전체/KIS/NH·VR] | ✅ |
| P5 | 실계좌 드라이런 — 미리보기 ↔ 라오어 공개표 대조 | ✅ 49주차 22행 센트 일치 |
| P6 | 운영 반영 | ✅ **2026-08-22 첫 제출 22건 접수 완료** |

> TQQQ 충돌 우려는 **비해당**으로 판명 — VR은 NH 별도 계좌 2개(…635/…977)라 KIS 3전략과
> 티커가 겹쳐도 계좌가 분리돼 물량 간섭이 없다.

### 현재 운용 상태
- 자동 제출: **수동**(양 기수) — 몇 주기 공개표 대조 후 자동 전환 예정
- 자동 전환 시: 매주 **토 10:30 KST** 해당 주 시작 기수 1개를 산출·제출
- 주기 엇갈림: 5기 49주차(8/24~9/4) → 0기 258주차(8/31~9/11) → 5기 51주차(9/7~9/18) …

---

## 9. 참고
- 사용자 제공 VR 5.0 개요(라오어 카페) + 팬딩 공개표. 트렌치형 3전략과 별개 알고리즘.
- NH PLUG OpenAPI: `https://www.nhplug.com/openapi-docs/gbstock/openapi.json`
- 관련 문서: `NH_PLUG_멀티계좌_개발안.md`, `매매규칙_싸이클_회차.md`
- 코드: `strategies/vr/{vr_logic,nh_client,worker,models,main}.py`
