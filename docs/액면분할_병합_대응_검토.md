# 액면분할·병합 대응 검토서

> 상태: **검토 완료 · 미구현** (2026-09-18)
> 범위: 무한매수법 · 떨사오팔 · 종사종팔 (KIS) + VR 0기·5기 (NH)
> 방법: 운영 DB 4개 전 테이블·컬럼 전수 분류 + 체결 적재·싸이클 판정·감사·화면 코드 경로 추적

---

## 0. 결론

1. **현재 대응 코드 없음.** 분할·병합 감지, 소급수정, 주문 차단 전부 없다.
2. **감지만으로는 부족하다.**
   - 분할 전에 사서 분할 후에 파는 포지션은 기록이 두 단위로 섞여 **싸이클 판정·매도가·감사가 영구히 틀어진다** → 소급수정 필수
   - VR은 2주짜리 예약주문이 **이미 증권사에 걸려 있어** 분할 당일 개장과 동시에 옛 가격으로 체결된다 → 사후 감지로는 못 막는다. **공시 시점 사전 등록**이 1차 방어선이어야 한다
3. **치명 결함 4건** (§2). 이 중 C2는 병합 한 번이면 무한매수법 싸이클이 영원히 안 닫힌다.
4. **소급수정은 "분할일을 가로지르는 것만" 하면 된다** (§4). 이미 닫힌 싸이클은 전부 분할 전 단위로 일관돼 있어 손댈 필요가 없고, 원본을 보존해야 증권사 거래명세와 대조할 수 있다.
5. **병합이 분할보다 어렵다.** 트렌치 구조 자체가 성립하지 않는 경우가 생긴다 (현재 SPXL 트렌치 2주씩 → 1:10 병합 시 전 트렌치 소멸). 사용자 결정 필요 (§9).

---

## 1. 데이터 분류 — 무엇이 바뀌고 무엇이 안 바뀌나

분할 비율을 **r** (2:1 분할 r=2, 1:10 병합 r=0.1)이라 하면
**주당 가격 ÷ r · 주식 수 × r · 달러·비율·T값 불변.**

### 불변 🟢 (수정 불필요)

| 대상 | 이유 |
|---|---|
| 금액 컬럼 전부 — amount, cum_buy/sell_amount, total_buy/sell_amount, profit, seed, B, total_usd, amount_per_tranche, quarter_base_cash, initial_holdings_cost | 달러 |
| 비율 — R, x_pct, star_pct, profit_pct, buy_limit_pct | % |
| **T값** | `보유원가 ÷ B` — 원가(달러) 불변 |
| VR — V, 밴드, Pool, E, 적립/인출, weekly_history 전부 | 달러 |
| core — 자산추이, 현금흐름, 시드할당, 복리상태 | 달러 |

### 가변 (주당 단위)

| 전략 | 테이블.컬럼 | 쓰임 | 방치 시 |
|---|---|---|---|
| 무한매수법 | portfolio_states.avg_price, qty | 주문가·수량·쿼터 1/4 | 🟢 매 동기화 KIS 잔고로 덮어씀 |
| | trades.price, qty, order_price | **싸이클 종료 판정(수량합)**, 회차 표시, 감사 | 🔴 C2 |
| | orders.price, qty | 이력 표시 | 🟡 |
| 떨사오팔·종사종팔 | tranches.avg_price, buy_price, qty | **매도가, 전체평단, 매도수량** | 🔴 C3 · H1 |
| | trades.price, qty | 감사(수량합), 표시 | 🟠 H2 |
| | trade_orders.price, qty | 당일 미체결 중복방지 매칭, 표시 | 🟡 |
| VR | gisu.model_qty, unit | **사다리 가격 = 밴드 ÷ 잔여** | 🔴 C1 |
| | fills.price, qty_acct, **dedup_key** | 모델 반영, 중복판정 | 🔴 C4 |
| | reserved_orders.price, qty_acct | 원장, 경고배너 | 🟡 |
| | acct_snapshot.qty, close | 표시 | 🟢 매 동기화 덮어씀 |
| core | _holdings.json | 표시 | 🟢 매 동기화 덮어씀 |
| | _t_audit.jsonl (avg, qty) | 감사 로그 | 🟡 이력 — 수정 안 함 |

---

## 2. 발견된 결함

### C1 🔴🔴 VR 예약주문이 옛 가격으로 일괄 체결

- 사다리 가격 = `밴드 ÷ 모델잔여`. 모델잔여(0기 430, 5기 216)는 DB값이라 분할을 모른다.
- 더 큰 문제: **2주 기간잔량 예약이 이미 NH에 걸려 있다** (현재 0기 260주차 19건, 5기 51주차 22건).
- **2:1 분할** (TQQQ $72 → $36): 매수 지정가 $56.96~60.65가 시가보다 높아 **매수 8단 개장 즉시 전량 체결**. 매도 $82~93은 영영 안 닿음.
- **1:10 병합** (→ $720): 매도 지정가 $82~93이 시가보다 낮아 **매도 14단 개장 즉시 체결 시도**.
- NH가 권리 발생 시 예약주문을 자동 취소하는지 **미확인** (§8). 확인 전까지는 체결된다고 가정해야 한다.
- 우리가 분할을 알아챌 수 있는 건 NH 잔고가 바뀐 뒤 = 개장 직전~직후. **사후 감지로는 늦다.**

### C2 🔴 무한매수법 — 병합 시 싸이클이 영원히 안 닫힘

`strategies/infinite/worker.py:919-934` `_check_cycle_end`

```python
net_qty = Σ매수수량 − Σ매도수량      # 현재 싸이클 trades
if net_qty > 0:
    return False                    # 아직 청산 전으로 판정
```

- 싸이클 중 1:10 병합 → 병합 전 매수 370주 기록 → 병합 후 37주 전량 매도 → net_qty = 370 − 37 = **333 > 0**
- 잔고는 0인데 **싸이클 종료로 인정 안 함** → 이력 미기록, 회차·T 리셋 안 됨, 복리 증액 안 됨, 다음 싸이클 최초매수 꼬임
- 2:1 분할은 net_qty가 음수라 통과 → **병합에서만** 터진다

### C3 🔴 트렌치 매도가 오산정

`strategies/ddsop/trading_logic.py:120` — `sell_price = tranche.avg_price × (1 + x%)`

- **2:1 분할**: DB 평단 $299 그대로, 실제가 ~$140 → 매도가 영영 안 닿음 → 익절 불가, 손절일까지 방치
- **1:10 병합**: DB 평단 $299 그대로, 실제가 ~$2,800 → 매도조건 즉시 충족 → **DB 수량(2주)으로 매도 주문 → 실제 보유 0.2주** → 거부 또는 오매도

### C4 🔴 VR 체결 중복판정 키가 "행 전체 해시"

`strategies/vr/worker.py:25-60` `_parse_fill` — `dedup_key = sha256(행의 모든 필드)`

- NH가 분할 후 과거 거래내역 단가·수량을 소급 재진술하면 **같은 체결의 키가 바뀐다** → 새 체결로 오인 → **모델잔여·Pool 이중 반영**
- 분할과 무관하게도 NH가 표시용 필드 하나만 바꿔도 같은 사고가 난다 → **분할 대응과 별개로 먼저 고쳐야 한다**
- 참고 — KIS 3전략은 이 문제가 **없다**:
  - 무한매수법: **주문번호(odno)**로 중복판정 (`infinite/worker.py:1083-1108`)
  - 떨사오팔·종사종팔: **미체결 주문번호 매칭 후 filled 전환** (`ddsop/worker.py:427-470`)
  - → KIS가 과거 체결을 소급 조정하든 말든 중복 적재는 없다

### C5 🔴 최소 1주 강제 매수 — 병합 후 예산 10배 초과 (시뮬레이션으로 발견)

세 전략 모두 매수수량을 `max(1, int(예산 ÷ 가격))`으로 계산한다. 주가가 예산보다 비싸도 **무조건 1주**를 산다.

- 평소엔 영향이 작다 (SOXL 현재 $133 × 1주 = 1회 매수금 B $125의 1.06배)
- **병합 후엔 폭발한다** — SOXL 1:10 병합 시 $1,330 × 1주 = **B의 10.6배**, SPXL 1:10 병합 시 $2,786 × 1주 = **트렌치 예산의 3.9배**
- 무한매수법이면 하루에 10회차를 한꺼번에 소진 → 며칠 만에 T=40 → 쿼터 진입
- 근본 문제: **병합 후 주가가 1회 매수금보다 훨씬 크면 분할매수 전략 자체가 성립하지 않는다.** SOXL $1,156에 시드 $5,000이면 전 재산으로 4주밖에 못 산다. 40분할을 유지하려면 시드가 $46,000 이상이어야 한다
- → 병합 시 **소급수정만으로는 해결 안 됨.** 시드·분할수 재검토 또는 해당 종목 운용 중단을 사용자가 결정해야 한다

### C6 🔴 무한매수법 분할수 A=40 하드코딩 + 쿼터 진입 구간 건너뛰기

병합 대응으로 **"시드·분할수 재설정"을 택했는데(§9 결정), 지금 코드로는 분할수(A)를 바꾸면 전략이 깨진다.**

| 항목 | 코드 | A를 따라가나 |
|---|---|---|
| 1회 매수금 B | `seed ÷ A` (`models.py:64`) | ✅ |
| 별% | `R − (T/2)×(40/A)` (`trading_logic.py:66`) | ✅ |
| **전반/후반 경계** | `if state.T < 20` (`trading_logic.py:167`) | ❌ **20 고정** |
| **쿼터 진입** | `39.1 <= state.T <= 40` (`trading_logic.py:397`) | ❌ **40 고정** |

- A=20으로 줄이면: T 최대 20 → 후반전 영영 안 옴, **쿼터(손절) 절대 진입 안 함**
- A=80으로 늘리면: 원금 1/4 지점(T=20)에서 후반전, **원금 절반 지점(T=39.1)에서 조기 손절**

**쿼터 구간 건너뛰기** (병합과 무관, 지금도 있는 결함) — 진입 조건이 `39.1 ≤ T ≤ 40` 구간 하나뿐이고 T>40을 쿼터로 보내는 경로가 없다. T는 0.1 단위 **올림**(`ceil(원가÷B×10)/10`)이라 원가 기준 실제 폭은 1.0 — **하루 증가폭이 1.0을 넘는 날(1주 가격 > B)** 구간을 건너뛰어 **영영 NORMAL로 남아 손절 없이 매수가 계속된다.**
- 병합 시뮬레이션: T 32.2 → 41.5 건너뜀 → 3일 만에 T 50.7, 원가 $6,338 > 시드 $5,000 (공용계좌 타 전략 자금 사용)
- 평소에도: SOXL은 주가가 B($125)보다 비싼 날 하루 증가폭이 1.0을 넘는다. T가 39.0~39.1에 멈춘 다음 날이면 건너뜀

→ **수정: 경계를 A에 연동** — 전반/후반 `T < A/2`, 쿼터 진입 `T ≥ A − 0.9` (상한 제거). 쿼터→NORMAL 복귀 조건과 얽혀 있어 검토 후 반영.

### C7 🔴🔴 쿼터 복귀 오판 — **2026-09-09~10 실제 발생, 이중 손절**

`strategies/infinite/worker.py` `_check_quarter_loc_sell` — 쿼터 step 1~10에서 "LOC 매도 체결"이 있으면 NORMAL 복귀. 그런데 세는 범위가 **싸이클 시작일(cycle_start_date) 이후 전체 LOC 매도**다. NORMAL 후반전에도 1/4 LOC 매도가 나가므로, 쿼터 들어가기 **전**의 매도가 세어져 쿼터 step 1이 되자마자 복귀한다.

SOXL 싸이클 9 실제 로그:

| 시각 (KST) | 일 |
|---|---|
| 9/9 05:01 | 쿼터 진입 (T=39.2) |
| 9/9 23:00 | MOC 1/4 매도 주문 → step 1 |
| 9/9 23:01 | "LOC 매도 체결 감지(1건)" → **NORMAL 복귀** — 세어진 건 쿼터 전 NORMAL 때 LOC 매도 |
| 9/9 23:03 | MOC가 아직 미체결이라 T=39.2 그대로 → **쿼터 재진입, step 0 리셋** |
| 9/10 23:00 | **MOC 1/4 매도 또 주문** |
| 9/10 23:01 | 또 NORMAL 복귀 |

- 체결: 9/9 MOC 8주 @ $125.87, **9/10 MOC 6주 @ $115.76** → 32주 → 18주 (44% 매도)
- V2.2 규칙상 1/4 손절은 1회, 이후 10회 분할매수(−R% LOC) + LOC 매도 1/4 + 지정가 3/4. 실제로는 **두 번째 손절(6주, 실현 −$223)이 나가고 쿼터 매수는 0회**
- 원인 둘: ① 복귀 판정이 쿼터 진입 **이후** LOC 매도만 세야 하는데 싸이클 전체를 셈 ② MOC 체결(장 마감) 전에 복귀해 T가 그대로라 즉시 재진입하며 step이 0으로 초기화

→ **수정**: 복귀 판정을 **쿼터 진입 시점 이후 체결된 LOC 매도**로 한정 (진입 시각 또는 진입 시점 trade id 기록). C6과 같은 상태머신이라 함께 수정·검증.

### H1 🟠 종사종팔 전체평단

`strategies/jongsa/trading_logic.py:138-161` — 일괄매도 기준 가중평균 평단을 **DB 트렌치**로 계산 → C3과 같은 방식으로 틀어짐. 자전거래 가드(매수가 ≤ 최저매도가×0.995)도 같이 틀어짐.

### H2 🟠 싸이클 정합 감사 오탐

`core/t_audit.py:194, 324` — 싸이클별 `매수수량합 ≠ 매도수량합`이면 불일치. 분할이 낀 싸이클은 소급수정 전까지 **매일 09:00 오탐**.

### M1 🟡 VR 단위(unit) 재정의

- 모델잔여만 ×r 하고 단위(0기 4, 5기 2)를 그대로 두면 한 칸 금액이 1/r로 바뀐다. 유지하려면 단위도 ×r.
- **병합(r=0.1)이면 단위 2 → 0.2주 — 정수 불가.** 라오어가 병합 후 모델을 어떻게 재정의하는지 따라야 한다 → **라오어 공개표 기준 재설정**.

### M2 🟡 경계일 정의 — 테이블마다 날짜 의미가 다르다

소급수정 대상 = **ex-date(미국 거래일) 이전 체결.** 그런데:

| 컬럼 | 의미 |
|---|---|
| infinite.trades.trade_date, ddsop/jongsa.trades.trade_date | KIS `ord_dt` (주문일) |
| infinite.orders.created_at 등 | **UTC** (`datetime.utcnow`) — 23:00 KST 워커 주문이 14:00으로 저장됨 |
| vr.reserved_orders.start_dt/end_dt | 미국 거래일 |

테이블별로 "이 날짜가 미국 몇 일 세션인가"를 확정해 경계를 적용해야 한다.

### L1 ⚪ 화면·기록

- 체결내역·싸이클 상세의 과거 단가가 분할 전 값 → 평단이 계단식으로 끊겨 보임 → 분할 표시선으로 처리
- 매매일지(블로그) 기발행분은 수정 불가 → 분할 이후 일지에 "○/○ 분할 1:r, 이전 단가 ÷r" 한 줄 명시

### 안전 확인 🟢

- 무한매수법 주문가·수량·T값·쿼터 1/4 수량 — 매번 KIS 잔고(평단·수량)에서 새로 계산
- KIS 3전략 체결 적재 중복판정 — 주문번호 기준 (C4 참고)
- 달러 기반 전부 — 시드, 복리, 자산추이, 현금흐름, 싸이클 손익

---

## 3. 현재 노출 (2026-09-18)

| 티커 | 증권사 | 전략 | 보유 | 1:10 병합 시 |
|---|---|---|---|---|
| **TQQQ** | KIS | 무한매수법 | 27주 | 2주 + 단주 현금 |
| **TQQQ** | NH | VR 0기 ×1 | 모델 430 · 단위 4 · 실보유 422 | 단위 0.4 — 정의 불가 |
| **TQQQ** | NH | VR 5기 ×6 | 모델 216 · 단위 2 · 실보유 1,308 | 단위 0.2 — 정의 불가 |
| SOXL | KIS | 무한매수법 | 23주 | 2주 + 단주 현금 |
| SPXL | KIS | 떨사오팔 | T1~T4 × 2주 = 8 | 트렌치당 0.2주 → **전 트렌치 소멸** |
| UPRO | KIS | 종사종팔 | T1~T7 × 9주 = 63 | 합계 6주 → **7개 트렌치에 배분 불가** |

**TQQQ 하나가 분할하면 두 증권사·세 전략이 동시에 영향**받는다. 처리는 전략 단위가 아니라 **티커 단위로 한 번에** 해야 한다.

---

## 4. 소급수정 범위 — "분할일을 가로지르는 것만"

**이미 닫힌 싸이클은 수정할 필요가 없다.** 매수·매도가 전부 분할 전 단위로 기록돼 그 자체로 정합하다 (수량합 일치, 달러 손익 정확). 깨지는 건 **분할 전에 사서 분할 후에 파는 열린 포지션**뿐이다.

| 구분 | 대상 | 처리 |
|---|---|---|
| **필수** (매매 영향) | 무한매수법 — 진행 중 싸이클의 ex-date 이전 trades | price ÷r, order_price ÷r, qty ×r |
| | 떨사오팔·종사종팔 — 보유 트렌치 | avg_price ÷r, buy_price ÷r, qty ×r |
| | 〃 — 보유 트렌치의 ex-date 이전 매수 trades | price ÷r, qty ×r |
| | VR — gisu | model_qty ×r, unit 라오어 기준 재설정 |
| | VR — 걸린 예약 | **취소 → 재산출 → 재제출** |
| | VR — 현 주기 fills | price ÷r, qty_acct ×r |
| | **병합 단주 현금정산** | **매도 체결로 기록** (수량 = 단주, 금액 = 정산액). 안 하면 소급해도 싸이클이 안 닫힘 — S2 실측 |
| **보존** (수정 안 함) | 닫힌 싸이클 trades·orders·trade_orders | 증권사 거래명세와 대조 가능하도록 원본 유지. 화면에 분할 표시선만 |
| | 달러·비율·T값, _t_audit.jsonl, 블로그 기발행분 | |
| | 무한매수법 portfolio_states | 다음 동기화가 KIS 잔고로 덮어씀 → 적용 직후 강제 동기화로 확인 |

---

## 5. 수정 방식

| | A안 — 값 직접 수정 | B안 — 조정계수 컬럼 |
|---|---|---|
| 방식 | 해당 행의 price·qty를 r로 고쳐 씀 | 원본 유지, 행마다 `adj_factor`, 합산 시 `qty × adj_factor` |
| 매매 로직 | **무변경** | 수량 합산 경로 전부 수정 (싸이클 종료·감사·가중평단·표시) |
| 누락 위험 | 없음 | 하나라도 빠지면 조용히 틀림 |
| 원본 | 백업으로 보존 | 테이블에 보존 |

**권고: A안.** §4처럼 필수 범위가 좁아서(열린 싸이클·보유분) B안으로 전 경로를 고칠 이유가 없고, 운영 시스템 최소변경 원칙에도 맞다.

- **병합 소수 문제**는 열린 싸이클 trades에서만 생긴다 (병합 전 7주 → 0.7주). 해당 행만 소수 보관 + 싸이클 종료 판정에 허용오차(`net_qty > 0.5`) — C2 수정 겸용.
- 전 과정 **이벤트 원장**(멱등) + 적용 전 **4개 DB·core 파일 스냅샷 백업** → 원클릭 롤백.

---

## 6. 설계 원칙

1. **사전 등록이 1차 방어, 감지는 2차 방어**
   - 레버리지 ETF 분할·병합은 보통 1~2주 전 공시된다 → 대시보드에 `(티커, ex-date, 비율)` 등록
   - 등록 시: ex-date 전 마지막 영업일에 해당 티커 **VR 예약 전량 취소**, ex-date 당일 **전 전략 신규주문 차단**
   - 놓쳤을 때 대비 **감지**: 매 동기화마다 DB 기대수량 vs 증권사 실제수량이 정수비로 어긋나고 가격도 같은 비율로 움직이면 → 해당 티커 전 전략 자동 정지 + 빨간 배너
2. **자동 적용 금지** — 감지돼도 소급수정은 사용자 확인 후. HTS 직접매매로도 수량이 어긋나기 때문 (예: 과거 UPRO 유령 9주). 오판 시 장부를 망가뜨린다
3. **멱등성** — 이벤트 상태 `registered → halted → applied → resumed`, 같은 이벤트 이중 적용 차단
4. **원자성** — 한 티커의 모든 전략·DB를 한 번에. 하나라도 실패하면 전부 롤백
5. **티커 단위** — 증권사·전략을 가로질러 일괄 (TQQQ = KIS 무한매수법 + NH VR 2개)
6. **재개는 검증 후** — 적용 → 강제 동기화 → DB 수량 = 증권사 수량 확인 → 미리보기 가격이 현재가 근처인지 확인 → 사용자가 킬스위치 해제

---

## 7. 처리 절차

| 시점 | 할 일 |
|---|---|
| 공시 확인 | 대시보드에 분할 등록 (티커 · ex-date · 비율) |
| ex-date −1 영업일 | 해당 티커 VR 예약 전량 취소 (NH `reservedCancel`), 전 전략 신규주문 차단 |
| ex-date 당일 | 주문 없음. 동기화만 → 증권사 잔고 반영 확인 |
| 반영 확인 후 | 실제 수량·평단으로 비율 재확인 → 백업 → 소급수정 → 강제 동기화 → 정합 확인 |
| 사용자 확인 | 미리보기 확인 → 킬스위치 해제. VR은 라오어 공개표로 모델 재설정 후 재제출 |

---

## 8. 미확인 사항 (실측 또는 증권사 문의 필요)

1. **NH·KIS가 분할 시 걸린 주문을 자동 취소하는가** — 특히 NH 기간예약주문. 취소해준다면 C1 위험이 크게 준다
2. **증권사 잔고 반영 시점** — ex-date KST 아침? 미국 개장 시? 23:00 주문 워커보다 먼저인가
3. **과거 체결내역 소급 재진술 여부** — KIS `inquire-ccnl`, NH `dailyTransaction` (KIS 3전략은 무관, VR은 C4 수정 후 무관)
4. **전일종가가 수정주가인가** — ex-date에 떨사오팔 매수가(`전일종가×(1−x%)`)와 무한매수법 매수캡이 수정 전 종가를 쓰는지
5. **권리 입고/출고 행 형태** — VR `_parse_fill`이 분할 당일 거래내역을 매수/매도로 오인하지 않는지
6. ~~KIS 해외 권리(분할) 조회 API 존재 여부~~ → **✅ 있음, 실측 완료 (§11)**. 사전 등록 자동화 가능
7. **API 기준일과 실제 가격 반영일이 다른 경우** — GLL·SMDD는 API 기준일에 원주가 점프가 없었다 (§11). 최종 비율·적용일은 **증권사 잔고 수량 변화로 확정**해야 한다

---

## 9. 사용자 결정

### ✅ 결정됨 (2026-09-18)

- **병합 시 → 시드·분할수 재설정.** 운용 종료·병합 전 청산이 아니라 파라미터를 다시 잡아 계속 운용한다.
  - 전제: C6(A 하드코딩) 수정이 먼저 돼야 분할수를 바꿀 수 있다
  - 최소 기준: **1회 매수금 ≥ 1주 가격** (그 아래면 최소 1주 강제매수로 C5 재발)
- **진행 중 싸이클 → (가) 새 시드로 이어가기.** 싸이클을 끊지 않고 새 시드·분할수로 T를 재계산해 계속 운용
  - 예: SOXL 병합 후 원가 $2,867, 시드 $46,252·A 40이면 B $1,156 → **T 23.0 → 2.5** (후반전 → 전반전 전환, 물타기 재개)

### 미결

1. **병합 시 트렌치 단주 처리** — 예: UPRO 7트렌치 × 9주 → 1:10 병합 후 6주
   - (가) 트렌치 수를 줄여 재구성 (6트렌치 × 1주 등)
   - (나) 해당 싸이클 강제 종료 (현재가 정산 기록) 후 새 싸이클
   - (다) 단주는 마지막 트렌치부터 깎고 현금정산분은 실현손익 처리
2. **VR 병합 시 모델 재설정** — 라오어 공개표를 기다려 수동 재설정 / 임시로 단위 반올림 운용
3. **소급수정 방식** — A안 (권고) / B안
4. **C4를 분할 대응보다 먼저 고칠지** — 분할과 무관한 잠재 버그

---

## 10. 반영 리스트 (2026-09-18 개정 — 승인 후 순서대로)

모든 매매 로직 변경은 **운영 DB 스냅샷 복사본 + 운영 함수 그대로 호출하는 시뮬레이션으로 수정 전/후를 대조**한 뒤 배포한다.

### 🔴 1단계 — 지금 운영 중인 결함 (분할과 무관, 최우선)

| # | 항목 | 결함 | 규모 |
|---|---|---|---|
| 1 | **쿼터 복귀 판정을 쿼터 진입 이후 LOC 매도로 한정** | C7 — 8/12~13·9/9~10 이중 손절 실제 발생 | ✅ **배포 2026-09-18 23:30** (§12) |
| 2 | **쿼터 진입 상한 제거 + 전반/후반·쿼터 경계를 A에 연동** | C6 — 구간 건너뛰면 손절 없이 계속 매수 / A 변경 불가 | ✅ **배포 2026-09-18 23:30** (§12) |
| 3 | VR 체결 중복판정 키를 행 전체 해시 → NH 거래 식별번호로 | C4 — 재진술 시 모델잔여·Pool 이중 반영 | ✅ **배포 2026-09-19 19:53** (§13) — 확인 중 C8 발견·함께 수정 |

1·2는 같은 상태머신(진입·단계·복귀)이라 **함께 수정·검증**한다. 3은 VR 체결이 아직 0건이라 이번 기회에 교체.

### 🟠 2단계 — 분할·병합 1차 방어 (사전)

| # | 항목 | 비고 |
|---|---|---|
| 4 | ICE 권리종합 일일 조회 → 보유 티커에 `주식분할`·`주식병합` 뜨면 자동 등록 + 빨간 배너 | 2~4주 전 감지 (§11.1) |
| 5 | 적용일 전 영업일: 해당 티커 VR 예약 전량 취소 / 적용일: 해당 티커 전 전략 신규주문 차단 | 비율 없이 적용일만으로 작동. C1·C5 차단 |

### 🟡 3단계 — 분할·병합 처리

| # | 항목 | 비고 |
|---|---|---|
| 6 | 수량 불일치 감지 → 자동 정지 | 4를 놓쳤을 때 2차 방어 |
| 7 | 소급수정 엔진 — 이벤트 원장(멱등) · 스냅샷 백업 · 열린 포지션만 수정 · **단주 현금정산 매도 기록** · 싸이클 종료 허용오차 · 롤백 | §4, C2·C3·H1·H2 |
| 8 | 병합 재설정 화면 — 기준값 제시(§11.5) → 시드·분할수 입력 → **(가) 이어가기**로 T 재계산 | 2 선행 필수 |
| 9 | VR 병합 시 모델 재설정 (라오어 공개표 기준 잔여·단위) | M1 |

### ⚪ 4단계 — 품질

| # | 항목 | 비고 |
|---|---|---|
| 10 | 분할 시뮬레이션 스크립트 저장소 편입 → 회귀 테스트 | 7·8 배포 전 필수 검증 도구 |
| 11 | 미국 휴장일 하드코딩 → KIS `countries_holiday` API | 9/7 노동절 사고 재발 방지 근본책 |

### 미결 결정 (8번 착수 전)

- **트렌치 전략 병합 시 단주** — 예: UPRO 7트렌치 × 9주 = 63주 → 1:10 병합 → 6주 + 0.3주 현금. (가) 이어가기를 트렌치에 적용하려면 재배분 규칙이 필요
  - 안: 보유 수량을 합쳐 새 트렌치 수로 재배분 (6주 → 6트렌치 × 1주, 평단은 가중평균 유지), 단주는 현금정산 매도로 기록

---

## 11. 실측 결과 (2026-09-18)

### 11.1 사전 공지를 API로 읽을 수 있는가 — ✅ 가능

KIS 공식 저장소(`koreainvestment/open-trading-api`)에서 해외 권리 API 2개를 찾아 운영 계정으로 직접 호출했다.

| API | TR / 경로 | 주는 것 | 용도 |
|---|---|---|---|
| **권리종합 (ICE)** | `HHDFS78330900` `/uapi/overseas-price/v1/quotations/rights-by-ice` | 종목별 **발표일·적용일(lock_dt)·권리종류**. **미래 이벤트 포함** | **사전 감지** |
| **기간별권리조회** | `CTRGT011R` `/uapi/overseas-price/v1/quotations/period-rights` | 기준일·**배정률(비율)**·확정여부 | **사후 비율 확정** |

**검증 — 알려진 과거 사례가 정확히 잡히는가**

| 종목 | 이벤트 | ICE 발표일 | ICE 적용일 | 선행 | 기간별 배정률 |
|---|---|---|---|---|---|
| SOXL | 15:1 분할 | 2021-02-01 | 2021-03-02 | **29일** | `1500` |
| TQQQ | 2:1 분할 | 2021-12-23 | 2022-01-13 | **21일** | `200` |
| NVDA | 10:1 분할 | 2024-05-23 | 2024-06-10 | **18일** | `1000` |
| KORU | 1:10 병합 | 2025-01-13 | 2025-02-10 | **28일** | `10` |
| SBIT | 1:5 병합 | 2025-05-29 | 2025-06-12 | **14일** | `20` |

- ICE `ca_title`은 `주식분할` / `주식병합` 문자열로 온다
- ICE `lock_dt` = 기간별권리조회 `bass_dt` = 실제 적용일 (5건 전부 일치)

**배정률 해석 — `비율 r = 배정률 ÷ 100`** (분할·병합 공통)

분할은 `200`=×2, `1500`=×15로 명확. 병합은 `20`이 1:5인지 1:20인지 애매해서 **원주가(`MODP=0`, 수정주가 미반영) 점프**로 교차검증했다.

| 종목 | 배정률 | 전일 → 당일 원주가 | 점프 | 판정 |
|---|---|---|---|---|
| KORU | 10 | $4.01 → $42.38 | ×10.57 | 1:10 ✅ |
| **SBIT** | **20** | $6.84 → $35.38 | **×5.17** | **1:5** ✅ (1:20 아님) |
| GLL | 50 | $20.65 → $21.24 | ×1.03 | ⚠️ 점프 없음 |
| SMDD | 50 | $16.53 → $17.29 | ×1.05 | ⚠️ 점프 없음 |

→ SBIT로 `÷100` 해석 확정. 단 **GLL·SMDD는 API 기준일에 가격 점프가 없었다** — 기준일이 실제 반영일과 다르거나 데이터 불일치. **API 값을 그대로 믿고 소급하면 안 되고, 최종 비율은 증권사 잔고 수량 변화로 확정**해야 한다 (§8-7).

**한계**
- 기간별권리조회는 **기준일이 지나야 등록**된다 (8월 병합 84건·9월 48건인데 9/18 이후는 0건) → 사전 공지용으로는 못 씀
- ICE는 **비율을 주지 않는다** → 적용일은 미리 알지만 비율은 적용일 이후에 확정

**결론** — 1차 방어(전날 VR 예약 취소 + 당일 주문 차단)는 **적용일만 있으면 되므로 ICE로 완전 자동화 가능**. 비율이 필요한 소급수정은 적용일 이후라 기간별권리조회·증권사 잔고로 확정하면 된다. 시점이 딱 맞아떨어진다.

**현재 우리 종목** (ICE ±3개월): 분할·병합 **없음**. SOXL·SPXL·UPRO 현금배당(기준일 9/22~23)만 있음. 시장 전체 향후 60일도 기간별권리조회상 0건(사후 등록 특성상 당연).

### 11.2 분할 시뮬레이션 — 운영 코드 그대로, 운영 DB 복사본에서

2026-09-18 21:30 운영 DB 4개를 **SQLite 백업 API로 읽기전용 스냅샷** → 로컬 복사본에서 운영 모듈 함수(`generate_orders`, `_check_cycle_end`, `buy_ladder`/`sell_ladder`, 싸이클 정합 감사)를 **그대로 import해 호출**. 시나리오마다 스냅샷을 새로 복사해 상호 오염 없음. 운영 DB·주문 변경 없음.

**S1. TQQQ 2:1 분할** ($71.66 → $35.83)

| 대상 | 소급 없음 | 소급 후 |
|---|---|---|
| 무한매수법 주문 | 🟢 KIS 잔고 반영만으로 가격 ½·수량 ×2 자동 조정 (매도 27주 → 54주) | — |
| VR 0기 사다리 | 🔴 매수 8단 $59~63 > 시가 → **개장 즉시 전량 체결 $1,950** | 🟢 $29.5~31.5, 즉시체결 0단 |
| VR 5기 사다리 | 🔴 매수 8단 $57~61 > 시가 → **개장 즉시 전량 체결 $5,642** | 🟢 $28.5~30.3, 즉시체결 0단 |
| 싸이클 3 종료 시 감사 | 🟠 종료는 됨, **감사 mismatch** (매수 103주 / 매도 130주) | 🟢 206 / 206, 감사 ok (체결 114건 소급) |

**S2. SOXL 1:10 병합** ($115.63 → $1,156.30) — 싸이클 9, 23주 → 2주 + 단주 0.3주 현금정산

| 처리 | 매수합 − 매도합 | 싸이클 종료 |
|---|---|---|
| 소급 없음 | 50 − 29 = **+21** | 🔴 영구 미종료 (C2) |
| **소급만** | 5 − 4.7 = **+0.3** | 🔴 **여전히 미종료** |
| 소급 + 단주 현금정산을 매도로 기록 | 5 − 5 = 0 | 🟢 종료 |
| 병합 후 매수 주문 | $1,329.74 × 1주 = **B의 10.6배** | 🔴 C5 |

→ **소급만으로는 부족하다.** 병합 단주 0.3주는 증권사가 현금으로 정산해 매도 체결이 없으므로, 이걸 매도로 기록하지 않으면 원장상 0.3주가 영원히 남는다. §4에 반영.

**S3. UPRO 2:1 분할** ($146.47 → $73.23) — 종사종팔 트렌치 7개

| | DB 수량 | 매도 LOC | 판정 |
|---|---|---|---|
| 소급 없음 | 63주 (실제 126) | $159.88 × 63주 | 🔴 시가 $73의 2.2배, 영영 안 닿음 |
| 소급 후 | 126주 | $79.94 × 126주 | 🟢 |

**S4. SPXL 1:10 병합** ($279.98 → $2,799.82) — 떨사오팔 트렌치 4개 × 2주

| 주문 | 내용 | 판정 |
|---|---|---|
| 매도 T1~T4 | LOC $287~301 × 2주씩 — 시가보다 낮아 즉시 조건 충족 | 🔴 실제 보유 0주(전량 단주 현금정산) → 거부 |
| 매수 | LOC $2,785.82 × 1주 | 🔴 **트렌치 예산 $714의 3.9배** (C5) |
| 소급 | 트렌치당 0.2주 → 정수 표현 불가 | 사용자 결정 필요 (§9-1) |

### 11.3 실측으로 바뀐 결론

1. **사전 감지는 자동화할 수 있다.** 사용자가 공시를 챙길 필요 없음 (ICE 일일 조회)
2. **병합 단주 현금정산을 매도로 기록하는 단계가 필수**로 추가됨 (S2)
3. **C5 신규** — 병합은 DB 문제를 넘어 **전략 성립 여부** 문제다. 소급수정 전에 시드·분할수·운용 지속 여부부터 결정해야 한다
4. 분할(r > 1)은 소급수정만으로 전부 정상화됨을 확인 (S1·S3). 어려운 건 병합(r < 1)

### 11.4 분할(정배수)에서도 소수점이 나오나 — 실측

KIS 기간별권리조회로 **2021-01 ~ 2026-09 미국 액면분할 511건 전수** 비율 분포:

| 구분 | 건수 | 비율 |
|---|---|---|
| 정수배 (2:1, 3:1, 10:1 …) | 484 | 95% |
| 소수배 (3:2, 5:4 …) | 27 | 5% — 대부분 ×1.5 (3:2) 16건 |
| **레버리지·인버스 ETF** | **81** | **소수배 0건** |

레버리지 ETF 분할 비율: ×2(27) ×3(15) ×4(12) ×5(8) ×6(2) ×7(1) ×8(3) ×10(6) ×15(2) ×20(2) ×25(2) ×100(1) — **전부 정수배**.

- 정수배 분할은 `수량 × 정수 = 정수` → 계좌·트렌치·VR 단위 어디서도 **소수점이 안 나온다**
- 소수배 분할(예: 3:2)이면 `27주 × 1.5 = 40.5주` → 단주 0.5주 현금정산 → 병합과 같은 처리 경로
- 우리 종목(TQQQ·SOXL·SPXL·UPRO)은 전부 레버리지 ETF라 5년간 소수배 전례 없음. 그래도 방어 코드는 **정수배 여부로 분기**하지 말고 **"결과 수량이 정수가 아니면 단주 처리"** 로 일반화한다

### 11.5 병합 시 재설정 기준값 (현 보유 기준, 1:10 가정)

최소 조건 **1회 매수금 ≥ 1주 가격** — 이 아래면 최소 1주 강제매수(C5).

| 종목 | 전략 | 병합 후 주가 | 현재 | A·트렌치수 유지 시 필요 시드 | 시드 유지 시 가능 분할수 |
|---|---|---|---|---|---|
| SOXL | 무한매수법 | $1,156 | 시드 $5,000 · A 40 | **$46,252** | A **4** |
| TQQQ | 무한매수법 | $717 | 시드 $5,000 · A 40 | **$28,664** | A **6** |
| SPXL | 떨사오팔 | $2,800 | 총액 $5,000 · 7트렌치 | **$19,599** | **1**트렌치 |
| UPRO | 종사종팔 | $1,465 | 총액 $10,000 · 7트렌치 | **$10,253** | **6**트렌치 |

1주 = 1회는 **하한선**이다. 매번 딱 1주씩이면 주가 따라 회차 금액이 들쭉날쭉하므로 실제로는 1회 2~3주 이상 여유를 두는 게 맞다.

---

## 12. 반영 1·2 — 쿼터 상태머신 수정 검증 (2026-09-18)

### 변경

| 파일 | 내용 |
|---|---|
| `infinite/trading_logic.py` | 쿼터 진입 `39.1 ≤ T ≤ 40` → **`T ≥ A − 0.9`** (상한 제거) · 전반/후반 `T < 20` → **`T < A/2`** |
| `infinite/worker.py` | `_mark_quarter_entry` 신설 — 진입 순간의 마지막 체결 id를 기준점으로 기록 (**체결 적재 뒤**에 호출) · `_check_quarter_loc_sell` 은 **기준점 이후 LOC 매도만** 셈, 복귀 시 기준점 초기화, 기준점 없으면 현재 id로 초기화(배포 전 진입분 대비) |
| `infinite/models.py` | `portfolio_states.quarter_entry_trade_id` 컬럼 + 기동 시 마이그레이션 |
| `infinite/main.py`, `core/blog_journal.py` | 화면·일지 문구의 전반/후반 경계를 A 연동 |

A=40(현 운영값)이면 T ≤ 40 구간의 동작은 이전과 **완전히 동일**하고, 달라지는 건 ① T>40에서 쿼터 진입 ② 쿼터 복귀 판정 범위 둘뿐이다. 현재 SOXL T=26.4 · TQQQ T=15.4 라 배포 즉시 바뀌는 주문은 없다.

### 검증 — 운영 DB 스냅샷 복사본 + 운영 함수 그대로 호출

같은 하네스를 수정 전/후 코드에 각각 실행. 워커의 쿼터 단계 증가 12줄만 복제(이번 수정 대상 아님), 나머지는 운영 함수 직접 호출.

**A. 9/9~9/17 SOXL 실사고 재현** — 실제 종가, 워커 하루 흐름(05:01 동기화 → 23:00 주문 → 23:01·23:03 동기화 → 장마감 체결)

| | 수정 전 | 수정 후 |
|---|---|---|
| 9/9 | MOC 8주 → 23:01 `Q→N`, 23:03 `N→Q0` 되돌이 | MOC 8주 → **Q1 유지** |
| 9/10 | **MOC 6주 (두 번째 손절)** | 쿼터 1단계: LOC 매수 1주(−12%) + LOC 매도 1/4 + 지정가 3/4 |
| 9/11~17 | NORMAL 매수 1주/일 | 쿼터 2~7단계 진행 |
| 결과 | **MOC 2회, 23주 — 운영 기록과 정확히 일치** (하네스 신뢰성 확인) | MOC 1회, 30주 |

**A-2. 8/11~8/20 두 번째 실사고 재현** — 수정 전 코드(직전 커밋)와 수정 후 코드에 같은 입력

| | 수정 전 | 수정 후 |
|---|---|---|
| 8/12 | MOC 6주 → `Q→N`, `N→Q0` 되돌이 | MOC 6주 → Q1 유지 |
| 8/13 | **MOC 5주 (두 번째 손절)** | 쿼터 1단계 매수 |
| 결과 | **MOC 2회, 8/20 보유 20주 — 운영 기록과 정확히 일치** | MOC 1회, 쿼터 1~6단계, 26주 |

두 사고 모두 원인은 **6/30 NORMAL 1/4 LOC 매도 1건(2주 @ $266.71)** — 싸이클 9의 유일한 LOC 매도가 두 번 다 쿼터를 즉시 풀었다. 싸이클 9 MOC 매도 4건이 전부 이틀 연속 쌍(8/12·13, 9/9·10)인 것도 이 버그의 흔적.

**B. 구간 건너뛰기** — T 38.9 → 40.1: 수정 전 NORMAL 🔴 / 수정 후 쿼터 🟢

**C. 분할수 변경** — A=20 (T 6·12·19.5), A=80 (T 30·39.6·79.5) 6개 경계: 수정 전 1/6 / **수정 후 6/6** 🟢

**D. 쿼터 중 회복** — 쿼터 LOC 매도($134.60) 체결 → NORMAL 복귀, T 22.1, 기준점 초기화, 즉시 재진입 없음 🟢

**E. 기준점 없는 기존 쿼터** — 과거 LOC 매도 무시하고 유지 → 이후 LOC 매도 체결 시 복귀 🟢

**F. 진입 당일 늦게 적재된 진입 전 LOC 매도** — 기준점을 적재 뒤에 찍어 진입 전으로 간주, 쿼터 유지 🟢

**G. A=40 경계 불변** — T 20.0 후반 · 39.0 후반 · 39.2 쿼터 (이전과 동일) / 40.1·41.6 쿼터 (이전엔 후반 — 건너뛰기 수정분)

앱 기동·마이그레이션·전 화면 200·SPA 렌더 확인.

### 배포 (2026-09-18 23:30:01 KST)

주문 워커(무한 :00 · 떨사 :15 · 종사 :25)를 피해 :30 배포. 전 화면 200, 운영 DB `quarter_entry_trade_id` 컬럼 추가, 배포 후 첫 동기화(23:31:58) 정상 — SOXL NORMAL T 26.4 · 23주, TQQQ NORMAL T 15.4 · 27주로 배포 전과 동일, 오류 0건.

**다음 확인 시점**: SOXL이 다시 쿼터 구간(T ≥ 39.1)에 들어갈 때 — MOC 1회 후 쿼터 1단계로 넘어가는지, 되돌이(`Q→N→Q`) 로그가 없는지.

### 관찰 (이번 범위 밖)

쿼터 1단계로 넘어갈 때 쿼터 매수금 `quarter_base_cash = min(B, 현금÷10)` 을 **MOC 체결 전**에 계산해 MOC 매도대금이 빠진다 (실측 $17.58). 최소 1주 강제(C5) 때문에 실제로는 1주씩 사서 체감 영향은 작지만, V2.2 의도와 다를 수 있다 — 별도 검토.

---

## 13. C8 🔴🔴 VR 체결 반영 미작동 — 반영 3 확인 중 발견 (2026-09-19 수정·배포)

**VR 체결 동기화가 인수(8/20) 이후 NH 체결을 한 건도 읽지 못하고 있었다.** 인수 뒤 실제 체결이 없어(마지막 TQQQ 체결 7/31) '체결 0건'이 정상처럼 보였을 뿐이다. 사다리가 한 칸이라도 체결되면 모델잔여·Pool 이 안 바뀐 채 다음 주기가 틀린 사다리로 자동제출될 상태였다.

| 원인 | 실측 |
|---|---|
| 종목 필터 `TQQQ` — NH 는 `TQQQ US` 형식 | 어느 기간이든 0건 (전체 조회는 20건씩) |
| 목록 블록 `Output_0` 대신 요약 `Output_1` 을 먼저 읽음 | `{입금총액 0, 출금총액 0, …}` 만 받음 |
| 20건에서 잘림 (00218) | 5기 7/1~8/31 이 20건에서 끊김 |

같은 계좌에 **AMDY(5기)·DHA VN(0기)** 도 있어 종목을 정확히 걸러야 한다.

**근본 원인** — 8/22 종가 오파싱 때 원인(칸 이름 추측)을 한 함수만 고치고 같은 유형을 전수 점검하지 않았다. VR 모듈 추측 파싱 7곳을 이번에 전부 명세 이름으로 교체(→ 0곳). KIS 3전략은 정확한 칸 이름을 써서 해당 없음.

**수정** — 거래내역 필터 없이 받아 `TQQQ US` 만 선별 · `Output_0` · 기간 분할 재조회 + 호출 한도 백오프 · 파서 명세 칸 이름(`act_trd_tp_nm`·`trd_qty`·`trd_uit_pr`·`trd_dt`·`trd_sno`) · 중복판정 키 `기수:거래일자-일련번호` · 잔고 `cns_bse_bnc_qty` · 일봉 `Output_1`·`close_prc` · 예약제출 접수번호 `bkg_rtn_orr_no` + 예약주문일자=시작일 기록(그동안 빈칸) · 미리보기 직전 체결 동기화.

**독립 감시 신설** — `NH 실보유 − 모델잔여×배수` 가 기준(`qty_offset`)에서 변하면 빨간 배너. 체결을 빠짐없이 반영하면 차이는 불변. 기준: 0기 −8주 · 5기 +12주 (인수 전 수동매매분, 7/31 이후 체결 없음을 NH 잔고 연쇄로 확인 후 설정).

**검증** — 서버에서 실제 NH 7월 체결 + 운영 VR DB 복사본 (NH 조회만)

| | 0기 | 5기 |
|---|---|---|
| 수정 전 체결 인식 | 0건 | 0건 |
| 수정 후 | 15건 (배당 제외) | 15건 (배당·AMDY 제외) |
| NH 잔고 연쇄 | 358→422, 끊김 0 = 현재 422 | 1,104→1,308, 끊김 0 = 현재 1,308 |
| 모델 반영 | +64 = NH +64 · Pool 센트 일치 | +34×6 = NH +204 · Pool 센트 일치 |
| 반복 조회 / 누락 가정 | 추가 반영 0 / 경보 +4주 | 추가 반영 0 / 경보 +12주 |

배포 후 운영 동기화 정상(현재 주기 체결 0건), 누락 감시 두 기수 ok, 오류 0.

**관찰 (범위 밖)** — 부분체결 시 모델 단위 환산이 체결 건별 반올림이라(`round(수량÷배수)`) 12주가 3+9주로 나눠 체결되면 모델이 1단위 더 늘 수 있다. 누락 감시가 이 경우 경보를 띄우므로 조용히 틀리진 않는다.
